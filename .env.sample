# Root environment variables (if needed for scripts)
# Individual services have their own .env files

# PostgreSQL
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# Database names
DB_IDENTITY_COMMERCE=ecom_identity_commerce
DB_CATALOG_FULFILLMENT=ecom_catalog_fulfillment
